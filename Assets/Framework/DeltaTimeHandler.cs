using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DeltaTimeHandler : MonoBehaviour
{
	float ActualDeltaTime;          //Actual Deltaime recieved from the Time class
	float DeltaTimeMultiplier;      //Multiplier combined with the actual deltatime to provide time effects
	float UsedDeltaTime;            //Deltatime that will be used 

    // Start is called before the first frame update
	void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
